{
  "name": "xgplayer-dash",
  "version": "3.0.0-alpha.1",
  "description": "xgplayer plugin for MPEG-DASH",
  "main": "./lib/index.js",
  "module": "./es/index.js",
  "typings": "./types/index.d.ts",
  "sideEffects": [
    "*.css"
  ],
  "scripts": {
    "prepublishOnly": "npm run build",
    "release:alpha": "npm version prerelease --preid=alpha && npm publish --tag alpha --registry=http://registry.npmjs.org",
    "release:alpha-test": "npm publish --tag alpha-test --registry=http://registry.npmjs.org",
    "release:3.0": "npm version patch && npm publish --tag 3.0 --registry=http://registry.npmjs.org",
    "build": "npm run build:umd && npm run build:es && npm run build:lib",
    "build:umd": "rm -rf ./dist && cross-env NODE_ENV=production rollup -c rollup.config.js",
    "build:es": "rm -rf ./es && ../../node_modules/.bin/babel src --out-dir es",
    "build:lib": "rm -rf ./lib && npx cross-env BABEL_TARGET=commonjs ../../node_modules/.bin/babel ./src --out-dir lib",
    "watch": "cross-env NODE_ENV=development rollup -c rollup.config.js --watch"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:bytedance/xgplayer.git"
  },
  "keywords": [
    "MPEG-DASH",
    "mp4",
    "fmp4",
    "player",
    "video"
  ],
  "author": "zhangxin.frontend@bytedance.com",
  "license": "MIT",
  "unpkgFiles": [
    "dist"
  ],
  "dependencies": {},
  "peerDependencies": {
    "xgplayer": "3.0.0-alpha.89"
  }
}
